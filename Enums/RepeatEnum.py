from enum import Enum


class RepeatEnum(Enum):
    ONCE = "once"
    DAILY = "daily"
    WEEKLY = "weekly"
    MONTHLY = "monthly"
    YEARLY = "yearly"

    # EVERY_SUNDAY
    # EVERY_MONDAY
    # EVERY_TUESDAY
    # EVERY_WEDNESDAY
    # EVERY_THURSDAY
    # EVERY_FRIDAY
    # EVERY_SATURDAY

